{
    "$schema": "vendor/infection/infection/resources/schema.json",
    "source": {
        "directories": ["src"],
        "excludes": [
            // ext-swoole transport glue; exercised by the conformance suite, not units.
            "Server/SwooleServer.php"
        ]
    },
    "mutators": {
        "@default": true
    },
    "testFramework": "phpunit",
    "tmpDir": "build/infection",
    // Floors sit below the ~89% covered-MSI achieved here to gate regressions
    // while tolerating run-to-run timing variance (timeouts) between local and CI.
    "minMsi": 83,
    "minCoveredMsi": 85,
    "logs": {
        "text": "build/infection/infection.log",
        "html": "build/infection/infection.html",
        "summary": "build/infection/summary.log",
        // Uploads the MSI to the Stryker dashboard for the README badge. Only
        // runs on `main` when INFECTION_BADGE_API_KEY is present (CI secret).
        "stryker": {
            "badge": "main"
        }
    }
}
